<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam MASTER - Premium Exam Preparation</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="style.css">
</head>
<body class="gradient-bg">
    <!-- Header & Navigation -->
    <header class="site-header">
        <div class="container mx-auto px-4 py-3">
            <div class="flex justify-between items-center">
                <!-- Logo -->
                <a href="#" class="flex items-center space-x-2">
                    <div class="logo-icon">
                        <i class="fas fa-graduation-cap text-white text-lg"></i>
                    </div>
                    <span class="logo-text">Exam <span class="gradient-text">MASTER</span></span>
                </a>
                
                <!-- Desktop Navigation -->
                <nav class="desktop-nav">
                    <a href="#home" class="nav-link">Home</a>
                    <a href="#mocks" class="nav-link">Mock Tests</a>
                    <a href="#pdfs" class="nav-link">PDF Store</a>
                    <a href="#dashboard" class="nav-link">Dashboard</a>
                    <a href="#reviews" class="nav-link">Reviews</a>
                    <a href="#contact" class="nav-link">Contact</a>
                </nav>
                
                <!-- Auth Buttons -->
                <div class="auth-section" id="auth-buttons">
                    <button class="login-btn" id="login-btn">Login</button>
                    <button class="signup-btn" id="signup-btn">Sign Up</button>
                </div>
                
                <!-- User Menu (Logged In) -->
                <div class="user-section hidden" id="user-menu">
                    <div class="user-info">
                        <div class="user-avatar" id="user-avatar">U</div>
                        <span class="username" id="username">User</span>
                    </div>
                    <button class="logout-btn" id="logout-btn">Logout</button>
                </div>
                
                <!-- Mobile Menu Button -->
                <button id="mobile-menu-button" class="mobile-menu-btn">
                    <i class="fas fa-bars text-xl"></i>
                </button>
            </div>
        </div>
    </header>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="mobile-menu">
        <div class="mobile-menu-header">
            <span class="mobile-menu-title">Menu</span>
            <button id="close-mobile-menu" class="close-menu-btn">
                <i class="fas fa-times text-xl"></i>
            </button>
        </div>
        
        <nav class="mobile-nav">
            <a href="#home" class="mobile-nav-link">Home</a>
            <a href="#mocks" class="mobile-nav-link">Mock Tests</a>
            <a href="#pdfs" class="mobile-nav-link">PDF Store</a>
            <a href="#dashboard" class="mobile-nav-link">Dashboard</a>
            <a href="#reviews" class="mobile-nav-link">Reviews</a>
            <a href="#contact" class="mobile-nav-link">Contact</a>
            
            <div class="mobile-auth-section" id="mobile-auth-buttons">
                <button class="mobile-login-btn" id="mobile-login-btn">Login</button>
                <button class="mobile-signup-btn" id="mobile-signup-btn">Sign Up</button>
            </div>
            
            <div class="mobile-user-section hidden" id="mobile-user-menu">
                <div class="mobile-user-info">
                    <div class="mobile-user-avatar" id="mobile-user-avatar">U</div>
                    <div>
                        <div class="mobile-username" id="mobile-username">User</div>
                        <div class="mobile-user-email">View Profile</div>
                    </div>
                </div>
                <button class="mobile-logout-btn" id="mobile-logout-btn">Logout</button>
            </div>
        </nav>
    </div>

    <!-- Overlay -->
    <div class="overlay" id="overlay"></div>

    <!-- Main Content -->
    <main>
        <!-- Hero Section -->
        <section id="home" class="hero-section">
            <div class="hero-background"></div>
            <div class="container mx-auto px-4 relative z-10">
                <div class="hero-content">
                    <h1 class="hero-title">
                        Empower your preparation. <br>
                        <span class="gradient-text">Learn smart. Score high.</span>
                    </h1>
                    
                    <p class="hero-description">
                        Access high-quality PDFs and real exam-style mock tests designed to help you master competitive exams at an affordable cost.
                    </p>
                    
                    <div class="hero-cta">
                        <a href="#mocks" class="cta-btn primary-cta pulse-glow">
                            Start Mock Test ₹19 <i class="fas fa-arrow-right"></i>
                        </a>
                        <a href="#pdfs" class="cta-btn secondary-cta">
                            Buy PDF ₹39 <i class="fas fa-arrow-right"></i>
                        </a>
                    </div>
                    
                    <!-- Limited Time Offer -->
                    <div class="offer-banner">
                        <div class="offer-content">
                            <div class="offer-icon">
                                <i class="fas fa-bolt text-yellow-300"></i>
                            </div>
                            <span class="offer-text">Get any PDF for ₹39 — 24-Hour Offer!</span>
                            <div id="countdown" class="countdown-timer">23:59:59</div>
                        </div>
                        <div class="offer-progress">
                            <div class="progress-bar"></div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Metrics Section -->
        <section class="metrics-section">
            <div class="container mx-auto px-4">
                <div class="metrics-grid">
                    <div class="metric-item">
                        <div class="metric-number">100+</div>
                        <div class="metric-label">Mock Tests</div>
                    </div>
                    
                    <div class="metric-item">
                        <div class="metric-number">50+</div>
                        <div class="metric-label">Study PDFs</div>
                    </div>
                    
                    <div class="metric-item">
                        <div class="metric-number">100k+</div>
                        <div class="metric-label">Students</div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Mock Tests Section -->
        <section id="mocks" class="mocks-section">
            <div class="container mx-auto px-4">
                <div class="section-header">
                    <h2 class="section-title">Attempt Real Exam-Style Mocks</h2>
                    <p class="section-description">
                        Each mock has 100 MCQs | 120 Minutes | Instant Score
                    </p>
                </div>
                
                <div class="exam-info">
                    <p>Practice with our extensive collection of mock tests designed by experts to simulate real exam conditions.</p>
                </div>
                
                <!-- Mock Tests will be loaded here by mocktest.js -->
                <div id="mock-tests-container" class="exam-grid">
                    <!-- Mock tests will be dynamically loaded here -->
                </div>
            </div>
        </section>

        <!-- PDF Store Section -->
        <section id="pdfs" class="pdf-section">
            <div class="container mx-auto px-4">
                <div class="section-header">
                    <h2 class="section-title">PDF Store</h2>
                    <p class="section-description">
                        Comprehensive study materials for all competitive exams
                    </p>
                </div>
                
                <!-- PDFs will be loaded here by pdf.js -->
                <div id="pdfs-container" class="pdf-grid">
                    <!-- PDFs will be dynamically loaded here -->
                </div>
            </div>
        </section>

        <!-- Dashboard Section -->
        <section id="dashboard" class="dashboard-section">
            <div class="container mx-auto px-4">
                <div class="section-header">
                    <h2 class="section-title">My Dashboard</h2>
                    <p class="section-description">Track your progress and access your purchased content</p>
                </div>
                
                <div id="dashboard-content" class="dashboard-grid">
                    <!-- Dashboard content will be loaded here -->
                    <div class="dashboard-card">
                        <h3 class="dashboard-card-title">
                            <i class="fas fa-chart-line text-blue-400"></i>
                            Progress Overview
                        </h3>
                        <div class="dashboard-stats">
                            <div class="dashboard-stat">
                                <div class="stat-number">0</div>
                                <div class="stat-label">Tests Taken</div>
                            </div>
                            <div class="dashboard-stat">
                                <div class="stat-number">0%</div>
                                <div class="stat-label">Average Score</div>
                            </div>
                            <div class="dashboard-stat">
                                <div class="stat-number">0</div>
                                <div class="stat-label">PDFs Purchased</div>
                            </div>
                            <div class="dashboard-stat">
                                <div class="stat-number">0</div>
                                <div class="stat-label">Study Hours</div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="dashboard-card">
                        <h3 class="dashboard-card-title">
                            <i class="fas fa-book-open text-green-400"></i>
                            My Purchases
                        </h3>
                        <div id="purchases-list" class="purchases-list">
                            <p class="empty-state">No purchases yet. Start exploring our PDFs and Mock Tests!</p>
                        </div>
                    </div>
                    
                    <div class="dashboard-card">
                        <h3 class="dashboard-card-title">
                            <i class="fas fa-clock text-yellow-400"></i>
                            Recent Activity
                        </h3>
                        <div id="activity-list" class="activity-list">
                            <p class="empty-state">No recent activity.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Testimonials Section -->
        <section id="reviews" class="reviews-section">
            <div class="container mx-auto px-4">
                <div class="section-header">
                    <h2 class="section-title">What Our Students Say</h2>
                    <p class="section-description">
                        Join thousands of successful students who aced their exams with Exam MASTER
                    </p>
                </div>
                
                <div class="reviews-grid">
                    <!-- Testimonials will be loaded here -->
                </div>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer id="contact" class="site-footer">
        <div class="container mx-auto px-4">
            <div class="footer-content">
                <!-- Footer columns will be loaded here -->
            </div>
            
            <div class="footer-bottom">
                <p class="copyright">© 2025 Exam MASTER. All Rights Reserved.</p>
                <div class="footer-links">
                    <a href="#">Privacy Policy</a>
                    <a href="#">Terms of Service</a>
                    <a href="#">Refund Policy</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Modals -->
    <!-- Login Modal -->
    <div id="loginModal" class="modal">
        <div class="modal-content">
            <h2 class="modal-title">Login to Your Account</h2>
            <input type="email" placeholder="Email Address" id="loginEmail" class="modal-input">
            <input type="password" placeholder="Password" id="loginPassword" class="modal-input">
            <div class="modal-actions">
                <button class="modal-btn secondary" id="closeLogin">Cancel</button>
                <button class="modal-btn primary" id="submitLogin">Login</button>
            </div>
            <div class="modal-footer">
                <a href="#" class="modal-link">Forgot Password?</a>
            </div>
        </div>
    </div>

    <!-- Signup Modal -->
    <div id="signupModal" class="modal">
        <div class="modal-content">
            <h2 class="modal-title">Create an Account</h2>
            <input type="text" placeholder="Full Name" id="signupName" class="modal-input">
            <input type="email" placeholder="Email Address" id="signupEmail" class="modal-input">
            <input type="password" placeholder="Password" id="signupPassword" class="modal-input">
            <div class="modal-actions">
                <button class="modal-btn secondary" id="closeSignup">Cancel</button>
                <button class="modal-btn primary" id="submitSignup">Sign Up</button>
            </div>
        </div>
    </div>

    <!-- Payment Modal -->
    <div id="paymentModal" class="modal">
        <div class="modal-content">
            <h2 class="modal-title">Complete Your Purchase</h2>
            <div class="purchase-summary">
                <div class="purchase-item">
                    <span class="purchase-label">Item:</span>
                    <span class="purchase-value" id="paymentItem">Item Name</span>
                </div>
                <div class="purchase-item">
                    <span class="purchase-label">Price:</span>
                    <span class="purchase-price" id="paymentPrice">₹19</span>
                </div>
            </div>
            
            <div class="payment-form">
                <div class="form-group">
                    <label class="form-label">Card Number</label>
                    <input type="text" placeholder="1234 5678 9012 3456" id="cardNumber" class="form-input">
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label class="form-label">Expiry Date</label>
                        <input type="text" placeholder="MM/YY" id="expiryDate" class="form-input">
                    </div>
                    <div class="form-group">
                        <label class="form-label">CVV</label>
                        <input type="text" placeholder="123" id="cvv" class="form-input">
                    </div>
                </div>
            </div>
            
            <div class="modal-actions">
                <button class="modal-btn secondary" id="closePayment">Cancel</button>
                <button class="modal-btn primary" id="submitPayment">Pay Now</button>
            </div>
            
            <div class="payment-security">
                <i class="fas fa-lock"></i>
                <span>Secure Payment • SSL Encrypted</span>
            </div>
        </div>
    </div>

    <!-- Success Modal -->
    <div id="successModal" class="modal">
        <div class="modal-content">
            <div class="success-content">
                <div class="success-icon">
                    <i class="fas fa-check text-white text-2xl"></i>
                </div>
                <h2 class="success-title">Payment Successful!</h2>
                <p class="success-message" id="successMessage">Your purchase has been completed successfully.</p>
                <button class="success-btn" id="closeSuccess">Continue</button>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="login.js"></script>
    <script src="pdf.js"></script>
    <script src="mocktest.js"></script>
    <script src="admin.js"></script>
    
    <!-- Main App Script -->
    <script>
        // Initialize all modules when DOM is loaded
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize authentication
            if (typeof initAuth === 'function') {
                initAuth();
            }
            
            // Initialize PDF store
            if (typeof initPDFStore === 'function') {
                initPDFStore();
            }
            
            // Initialize mock tests
            if (typeof initMockTests === 'function') {
                initMockTests();
            }
            
            // Initialize dashboard
            if (typeof initDashboard === 'function') {
                initDashboard();
            }
            
            // Initialize UI components
            initUI();
        });

        // UI Initialization
        function initUI() {
            // Mobile menu functionality
            const mobileMenuButton = document.getElementById('mobile-menu-button');
            const closeMobileMenu = document.getElementById('close-mobile-menu');
            const mobileMenu = document.getElementById('mobile-menu');
            const overlay = document.getElementById('overlay');
            
            mobileMenuButton.addEventListener('click', () => {
                mobileMenu.classList.add('open');
                overlay.classList.add('active');
                document.body.style.overflow = 'hidden';
            });
            
            closeMobileMenu.addEventListener('click', () => {
                mobileMenu.classList.remove('open');
                overlay.classList.remove('active');
                document.body.style.overflow = 'auto';
            });
            
            overlay.addEventListener('click', () => {
                mobileMenu.classList.remove('open');
                overlay.classList.remove('active');
                document.body.style.overflow = 'auto';
            });
            
            // Countdown Timer
            function updateCountdown() {
                const countdownElement = document.getElementById('countdown');
                let time = 23 * 60 * 60 + 59 * 60 + 59;
                
                const timer = setInterval(() => {
                    const hours = Math.floor(time / 3600);
                    const minutes = Math.floor((time % 3600) / 60);
                    const seconds = time % 60;
                    
                    countdownElement.textContent = 
                        `${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
                    
                    if (time <= 0) clearInterval(timer);
                    else time--;
                }, 1000);
            }
            
            updateCountdown();
            
            // Smooth scrolling
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const targetId = this.getAttribute('href');
                    if (targetId === '#') return;
                    
                    const targetElement = document.querySelector(targetId);
                    if (targetElement) {
                        window.scrollTo({
                            top: targetElement.offsetTop - 80,
                            behavior: 'smooth'
                        });
                    }
                });
            });
        }

        // Modal functions
        function openModal(modal) {
            modal.style.display = 'flex';
            document.body.style.overflow = 'hidden';
        }
        
        function closeModal(modal) {
            modal.style.display = 'none';
            document.body.style.overflow = 'auto';
        }

        // Global utility functions
        window.utils = {
            formatPrice: (price) => `₹${price}`,
            showNotification: (message, type = 'info') => {
                // Implementation for showing notifications
                console.log(`${type.toUpperCase()}: ${message}`);
            },
            formatDate: (date) => {
                return new Date(date).toLocaleDateString('en-IN');
            }
        };
    </script>
</body>
</html>
